// src/utils/drawerUtils.ts

/**
 * Returns the ID of the map container where drawers should anchor.
 */
export function getMapTargetId(): string {
    return 'map'; // or 'map', or whatever your real div id is
}
